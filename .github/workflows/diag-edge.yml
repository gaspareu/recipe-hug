name: Diag edge (temporaire)

on:
  push:
    branches: [claude/recipe-chat-flows-tests-47b2eb]

jobs:
  diag:
    runs-on: ubuntu-latest
    steps:
      - name: Test clé Anthropic via diag-anthropic
        run: |
          echo "=== claude-sonnet-4-6 ==="
          curl -sS --max-time 30 "https://ifpqsyyvytfpossqycpc.supabase.co/functions/v1/diag-anthropic" | head -c 1500
          echo
